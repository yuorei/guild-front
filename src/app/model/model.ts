export type Board = {
    id: string;
    user_id: string;
    title: string;
    description: string;
    reward: string;
    endDate: string;
    lebel: string;
    max: number;
    min: number;
    imageURL: string;
    createdAt: string;
    updatedAt: string;
};